#!/usr/bin/env python

from __future__ import print_function
import os
import sys
from StringIO import StringIO
from Bio import SeqIO

# def ab1_to_fastq(ab1_in, fastq_out):
#     assert ab1_in.rstrip().endswith(".ab1"), "not an .ab1 file"
#     return SeqIO.convert(ab1_in.rstrip(), "abi", fastq_out, "fastq")

# for line in sys.stdin:
#     output = StringIO("")
#     sys.stdout.write(str(ab1_to_fastq (line, output)))

for line in sys.stdin:
    ab1_record = SeqIO.read(open(line.rstrip(), "rb"), "abi")
    print(ab1_record.format("fastq"))
